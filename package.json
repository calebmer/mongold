{
  "name": "mongold",
  "version": "0.0.1",
  "main": "dist/index.js",
  "scripts": {
    "compile": "mkdir -p dist/ && node_modules/.bin/babel lib --out-dir dist",
    "test": "node_modules/.bin/mocha -r node_modules/babel/register",
    "prepublish": "npm run compile"
  },
  "config": {
    "blanket": {
      "pattern": "lib"
    }
  },
  "dependencies": {
    "async": "^1.2.0",
    "debug": "^2.2.0",
    "is-my-json-valid": "^2.12.0",
    "lodash": "^3.9.3",
    "mongodb": "^2.0.33"
  },
  "devDependencies": {
    "babel": "^5.5.6",
    "mocha": "^2.2.5"
  }
}
